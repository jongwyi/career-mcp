# 자격증명 — 절대 커밋 금지
.env
.env.*
!.env.example

# 프로필 데이터 (개인정보)
profile-import-*.json
*.profile.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node (확장)
node_modules/
extension/dist/

# 도구
.gstack/

# OS
.DS_Store

# 프로필 데이터 (개인정보)
#
# 파일명 패턴으로 막으면 반드시 샌다 — career_profile.json 이 실제로 공개 저장소에
# 올라갔고, 그 다음엔 한글 파일명이 뚫렸다. 그래서 **루트의 모든 .json 을 막고**
# 필요한 것만 명시적으로 허용한다. 코드용 JSON 은 하위 디렉토리에 둔다.
/*.json
!/.markdownlint.json
